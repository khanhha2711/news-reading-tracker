services:
  postgres:
    image: postgres:16
    container_name: news-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: news_db
    ports:
      - '5432:5432'
    volumes:
      - news-pgdata:/var/lib/postgresql/data

volumes:
  news-pgdata:
